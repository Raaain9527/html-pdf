# 从一份简历出发——两天，借助 AI 从零到一完成一个工具

> **投递岗位**：凉屋游戏 · 游戏运营实习生
>
> **GitHub**：[github.com/Raaain9527/html-pdf](https://github.com/Raaain9527/html-pdf)
>
> **背景**：投递凉屋游戏时，我的简历是 HTML 写的。导出 PDF 后发现文字变成了不可选中的图片——企业的 ATS 简历解析系统读不到任何信息。市面上没有现成的工具能同时做到"矢量文字 + 长页面不分页"。于是我没有凑合，而是借助 AI，用了两天时间从零到一写了这个工具。
>
> **投递作品说明**：这不是一个"技术练习"项目——它来自我投简历时遇到的真实问题。工具覆盖 CLI 命令行、Web UI、桌面应用三种形态。以下记录的是我如何发现问题、做技术选型判断、借助 AI 放大效率、以及这段经历和游戏运营工作的关联。

---

## 目录

1. [从一份简历开始的工具](#1-从一份简历开始的工具)
2. [两天做了什么](#2-两天做了什么)
3. [三个关键选择](#3-三个关键选择)
4. [踩过的坑](#4-踩过的坑)
5. [AI 的使用方式](#5-ai-的使用方式)
6. [质量保障](#6-质量保障)
7. [能力迁移：从写工具到做运营](#7-能力迁移从写工具到做运营)

---

## 1. 从一份简历开始的工具

投凉屋游戏的简历是用 HTML + CSS 写的，排版用了 Flex 和 Grid 布局。在浏览器里一切正常，但 Chrome「另存为 PDF」之后：

1. **文字变成了图片**。中文文本无法被选中、复制、搜索——ATS 简历解析系统读不到我的信息，简历等于白投
2. **长页面被截断**。三屏长的内容被切成了三页 PDF，中间还有空白间隙
3. **排版偏移**。浏览器里居中对齐的模块，到了 PDF 里偏了 20px

试了一圈市面上的方案：**wkhtmltopdf**（2016 年后没更新过，Flex 布局直接坍塌）、**在线转换工具**（限量、加水印、需要上传到第三方服务器）、**浏览器打印对话框**（分页线完全不可控，标题刚好卡在切口上）。

这些方案有一个共同问题——它们不是"我需要的工具"。我是一个求职者，我只想把一份 HTML 干净地导出为矢量 PDF。需求简单到一句话就能说清楚，但就是没有现成的工具能做好。

于是我自己写了一个。不是"我要练技术"，是"我真的需要这个东西，现在就要用"。

---

## 2. 两天做了什么

项目在 6 月 3 日至 5 日间完成核心开发，活跃工作时间约 18-22 小时。每个版本的迭代都是因为"这个场景真的需要它"：

| 版本 | 做了什么 | 为什么加它 |
|------|------|------|
| v0.1–v0.3 | CLI 核心导出、批量处理、Web UI | 我需要导出简历；换电脑也能用；一次导出多份 |
| v0.4 | 水印平铺、PDF 密码、元数据 | 有些场景需要标注"内部文件"，有些文件需要保护 |
| v1.0–v1.1 | 桌面应用（Electron → Tauri 迁移） | 给别人用——双击打开就行。第一版 92MB，迁移后降到 20MB |
| v1.1.1 | 捆绑 Node.js、修复各种边界问题 | 让完全不懂命令行的人解压即用 |

MVP 从零到第一个可用的 PDF 大约 2 小时。之后的每个版本迭代在 1-3 小时之间。最大的一次改动是桌面框架从 Electron 迁到 Tauri——这背后有一个很有意思的判断，在第 3 节展开。

整个过程的节奏是：**先让核心链路跑通，然后每次只加一个功能，确保上一个版本稳定后再进下一步**。没有"先画完美架构图再写第一行代码"的阶段。

三种形态共享同一套核心引擎（约 150 行），各自只是入口不同：

```
CLI 命令行         Web UI           桌面应用 (Tauri)
(开发者/脚本)      (浏览器拖拽)      (双击即用)
     │                 │                 │
     └─────────────────┼─────────────────┘
                       │
               html2pdf-lib (~150行)
               (Puppeteer 渲染引擎)
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
       Chromium      pdf-lib      Node.js
       (页面渲染)    (后处理)      (便携版)
```

做三种形态不是为了"显得全面"——是真实使用场景不一样：CLI 给自己日常用，Web UI 给偶尔换电脑时用，桌面应用给完全不碰命令行的朋友用。

---

## 3. 三个关键选择

### 为什么不用浏览器另存为

我把自己的简历 HTML 分别扔给三个方案，实际对比输出效果：

| 方案 | Flex 布局 | Grid 布局 | 圆角/阴影 | 中文 | 结论 |
|------|------|------|------|------|------|
| Puppeteer (Chromium) | ✅ | ✅ | ✅ | ✅ | **选用** |
| wkhtmltopdf | ❌ 坍塌 | ❌ 坍塌 | ✅ | ✅ | 淘汰 |
| WeasyPrint | ✅ | ⚠ 部分 | ❌ 丢失 | ✅ | 淘汰 |

Puppeteer 是唯一一个排版和浏览器完全一致的方案。验证方式不是看文档对比表——是实际跑一次，看输出。代价是需要系统有 Chrome/Edge，但大部分 Windows 电脑都自带。

### 为什么桌面版从 Electron 换到了 Tauri

第一版用 Electron 做桌面应用，3 小时搭完——安装包 92MB。对于一个"导出 PDF"的小工具来说明显不对。

| | Electron 第一版 | Tauri 迁移后 |
|------|------|------|
| exe 大小 | 92 MB | 20 MB |
| 运行时占用 | 4 个进程 | 2 个进程 |
| 捆绑了什么 | 完整 Chromium | 系统自带的 WebView2 |

迁移花了 6 小时，中间遇到了一些文档写不清楚的坑（见第 4 节）。但如果重来一次，我还是会先做 Electron 版——不是因为 Electron 好，而是**不先看到 92MB 的成品摆在那里，我不会有动力去啃一份全新的框架文档**。有些事情不亲自踩一遍，光看对比表是不会信的。

### 为什么保留 Node.js 而不是全部用 Rust

水印、密码、元数据这些功能，在 JS 生态里两天就做完了。水印模块还做了一个实用细节：自动检测水印文字是否含中文，有中文就嵌入系统黑体，没有就用标准英文 Helvetica——用户不需要手动配置字体路径。

如果全部用 Rust 重写，预估 3-5 天。在"快速出产品"和"架构纯粹"之间，我选了前者。运营工作的逻辑一样：先上线验证效果，再考虑优化——而不是在方案阶段追求完美。

---

## 4. 踩过的坑

### Bug 1：文件对话框弹不出来（~3h）

点击「导出 PDF」，界面显示 loading，但 Windows 的文件选择框就是不出现。过几秒报错"取消选择"。

先确认前端调用已发出 → 在后台函数入口加日志 → 日志没输出，说明函数根本没收到调用。借助 AI 审查了整条代码调用链，发现是 Rust 的异步线程和 Windows 文件对话框的兼容性问题——对话框需要特定类型的线程支持，而当时的代码跑在错误的线程上。

修复：改一行代码（去掉 `async` 关键字）。一行代码，排查三小时。根因和我本科的城乡规划专业毫无关系——但排查的思路是相通的：假设 → 验证 → 缩小范围 → 定位问题。

### Bug 2：Web 端导出的 PDF 显示源代码（~15min）

上传 HTML 文件后导出，PDF 里是 `<html><head><style>...` 的源代码，不是渲染后的页面。

用 CLI 测试同样的文件，正常——排除渲染引擎问题。对比 Web 和 CLI 的流程差异，发现文件上传后服务器给它存了一个没有后缀名的临时文件名（如 `a1b2c3d4`），Chrome 打开时不知道这是 HTML，按纯文本渲染。

修复：存完之后重命名加 `.html` 后缀。一行代码。但这个问题暴露了一个常见盲区：**工具链中的某个环节做了你没预料到的处理**（上传组件自动去掉了文件后缀）。运营工作中——比如分析玩家数据时——同样需要这种"不被表面现象迷惑，追到上游找根因"的习惯。

---

## 5. AI 的使用方式

这个项目里 AI 不是"一键生成"——它在几个具体环节放大了我的效率。

**PRD 和代码框架**：我描述了核心需求，AI 产出 PRD 和初版代码。但我没有直接接受——AI 推荐用 wkhtmltopdf（最轻量），实测我的简历排版会坍掉，最终选了 Puppeteer。AI 提供的是一个可以立刻被批评和修改的起点，这比从零写快很多。

**代码审查定位问题**：第 4 节的 Bug 1 排查了一个多小时没找到根因。AI 在一分钟内并行检查了五层代码，定位了问题。不是 AI 比我更懂——是它能同时看到所有层，而我有盲区。

**迭代节奏**：大部分 Bug 的闭环是"描述现象 → AI 给方案 → 我验证 → 不行再描述 → AI 改方案"。短的十分钟，长的三小时。比传统的"Google → 看文档 → 试 → 失败 → 换关键词再搜"快一个数量级。关键不是 AI 每次都对——经常不对——而是反馈循环极短。

**AI 没做的事**：最终的技术选型是我实测对比出来的、产品形态（三种入口）是我决定的、Electron → Tauri 的迁移判断是我做的。AI 可以列对比表，但它不知道 92MB 的结果对一个用户意味着什么。

---

## 6. 质量保障

开发过程中用 8 份不同结构的 HTML 做了手工回归测试——Flex 多栏布局、Grid 瀑布流、中文长文、含 ECharts 图表的页面、纯表格页面、英文 Landing Page、含外链 CSS 的页面、混合图文排版。每改一个版本，至少用其中 3-4 份跑一遍确认没被破坏。

项目 git commit 记录完整可追溯。发布了 3 个 GitHub Release，每个附带 NSIS 安装包和便携版 zip。

坦率地说，目前还没有自动化测试。原因是两天的时间分配给"让核心功能跑通并覆盖主要场景"已经紧张——自动化测试是后续改进的明确方向。

---

## 7. 能力迁移：从写工具到做运营

这段经历看起来是一个技术项目，但底层的做事逻辑和游戏运营是相通的。

| 项目中的具体实践 | 对运营工作的意义 |
|------|------|
| 从"自己投简历遇到问题"出发，两天从零到一发版 | 运营需要发现问题的敏感度——不是等需求，是找需求。玩家社区里的一条抱怨、一个数据异常，都是优化的起点 |
| 三个方案实测对比后选 Puppeteer——不是看文档，是看输出 | 运营决策需要依据——做活动方案选 A 还是 B，应该有数据或测试支撑，不是凭感觉 |
| 做了 CLI / Web / 桌面三种形态 | 运营内容要考虑受众——核心玩家的公告和泛用户的推送，是两种语言、两种渠道 |
| 发现第一版 92MB 后果断迁移框架，6 小时 | 活动效果不好的时候，投入时间调整比硬撑更划算。关键是敢承认"这个方案不对"，然后找更好的 |
| 8 份 HTML 回归测试——改一处，验证多处 | 每次运营改动都要问"哪里可能被影响"——改了一个活动入口，会不会影响关联的奖励发放逻辑 |
| 把 AI 用到实际工作流里，不是"用过"，是交付了产品 | 简历里写"熟悉 AI 工具"的人很多。能说清楚 AI 在哪个环节帮了什么、省了多少时间、人做了哪些判断的人很少——后者才是运营岗需要的 |

加入凉屋游戏后，我想把这种"从真问题出发、快速验证、AI 加速"的工作方式带到运营一线。从写一个小工具到策划一次玩家活动，底层的做事逻辑是一样的：**发现真实问题 → 最小成本验证 → 迭代优化 → 让结果说话**。

---

*项目时间线：2026-06-03 至 2026-06-05，单人完成全部工作。产出 CLI 工具、Web UI、Tauri 桌面应用三种形态。活跃开发时间约 18-22 小时。GitHub 仓库：[github.com/Raaain9527/html-pdf](https://github.com/Raaain9527/html-pdf)*

*> 附：技术版详细记录（含全部 3 个 Bug 的排查过程、渲染引擎选型实测对比、Electron vs Tauri 迁移细节）见 `AI作品展示报告_html-pdf_v1.md`*
