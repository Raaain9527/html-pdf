# v1.3.0 更新说明

## 核心：预览系统重构（流式投影）

本版把预览从"浏览器内 iframe 近似渲染"重写为**流式投影**——后台用真实 Chrome 渲染，前端显示真实渲染结果，实现所见即所得。

### 新特性

- **流式投影预览**：后台常驻 Chrome 渲染，真实布局直接投射到预览区，预览与导出一致
- **双分辨率混合**：滚动/拖拽时用实时帧保持流畅，静止后自动切换到高清帧（清晰度翻倍）
- **断点感知自动适配**：打开页面后自动把视口调到内容宽度，并避开页面自身的响应式断点（例如两列布局不会塌成一列）
- **交互始终可用**：点击、填写、滚动、键盘输入直接作用于真实页面，无需切换模式
- **交互式导出**：预览中的交互状态（点击、填表等）会进入导出的 PDF
- **缩放控制**：适配面板 / 1:1 / Ctrl+滚轮图像缩放

### 修复

- 预览坍塌：修复"窗口化时视口宽度被压缩触发页面断点"和"预览视口高度与导出不一致"两个根因
- 单击误选中：修复点击坐标与截图并发导致的大范围误选中
- 拖选锚点：拖选文字从按下点开始，不再从上方开始

### 其他

- 项目目录整理：报告文档归档到 `docs/reports/`
- 架构决策记录（ADR）+ 术语表沉淀到 `docs/`

## 已知限制

- 使用 `vh` 单位的页面（如 `100vh`）在"长页面单页导出"下会分页（Chromium 打印布局的固有行为）
- 分发包包含捆绑的便携 Node.js 与依赖，安装包体积较大（约 180MB）

## 安装说明

- 下载 `HTML-to-PDF_1.3.0_x64-setup.exe`（NSIS）或 `HTML-to-PDF_1.3.0_x64_en-US.msi` 安装
- 需要系统已安装 **Google Chrome 或 Microsoft Edge**（导出/预览用）
- 安装后即可使用，无需单独安装 Node.js（已捆绑）
