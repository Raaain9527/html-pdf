# 从一份简历出发——2 天从痛点到一个可发布产品

> **投递岗位**：实习生
>
> **GitHub**：[github.com/Raaain9527/html-pdf](https://github.com/Raaain9527/html-pdf)
>
> **背景**：2026 年 6 月求职季，我需要频繁将 HTML 简历导出为 PDF 投递。但浏览器「另存为 PDF」产出的栅格图文字无法被 ATS（简历解析系统）识别，长页面还会被截断。市面上没有工具同时满足"矢量化 + 长页面单页 + 中文水印"——于是我自己写了一个。
>
> **投递作品说明**：这是一次从真实痛点到完整产品的实践记录。工具覆盖 CLI 命令行、Web UI、桌面应用三种形态，2 天内迭代了多个版本。AI 不是这个故事的主角——它是我在特定环节的"杠杆"。以下记录的是遇到的具体问题、做过的技术决策、以及踩过的坑。

---

## 目录

1. [痛点与动机：从一份简历开始](#1-痛点与动机从一份简历开始)
2. [时间线与产出](#2-时间线与产出)
3. [关键技术与决策](#3-关键技术与决策)
4. [产品形态：三种入口，一套引擎](#4-产品形态三种入口一套引擎)
5. [踩坑与调试：三个真实 Bug](#5-踩坑与调试三个真实-bug)
6. [AI 的使用方式](#6-ai-的使用方式)
7. [反思与后续](#7-反思与后续)

---

## 1. 痛点与动机：从一份简历开始

投第一份简历时我就遇到了问题。

我的简历是用 HTML + CSS 写的，排版用了 Flex 和 Grid，在浏览器里看起来没问题。但当我用 Chrome「另存为 PDF」导出后，三件事同时出错了：

1. **文字变成了图片**。PDF 里的中文文本无法被选中、复制、搜索——这意味着企业的 ATS 简历解析系统读不到我的信息，简历等于白投
2. **长页面被截断**。三屏长的页面被硬生生切成三页 PDF，中间还有空白间隙
3. **排版偏移**。原本在浏览器里居中对齐的模块，到了 PDF 里偏左了 20px

我试了市面上能想到的方案：

- **wkhtmltopdf**：拿简历实测，Flex 布局直接坍塌，两个并列卡片变成上下堆叠。一查才知道这个项目 2016 年后就没更新过
- **在线转换工具**：免费版加水印、限制页数、而且我不想把自己的简历上传到第三方服务器
- **浏览器打印对话框**：可以调边距和缩放，但页面分割线完全不可控——h2 标题刚好卡在分页线上，一半在上一页一半在下一页

这些方案有一个共同问题：它们不是我需要的工具——我是一个求职者，我只想把一份 HTML 干净地变成矢量 PDF。这个需求简单到一句话就能说清楚，但就是没有现成的工具能做好。

于是我自己写了。不是"我要练技术"，是"我真的需要这个东西，现在就要用"。

---

## 2. 时间线与产出

项目在 2026 年 6 月 3 日至 5 日间完成核心开发，活跃工作时间约 18-22 小时。以下是 git commit 记录可追溯的版本演进：

| 版本 | 时间（commit 记录） | 做了什么 | 可用的产出 |
|------|------|------|------|
| v0.1–v0.3 | 06/03 白天（无独立 commit，squash 并入首个提交） | CLI MVP、批量导出、URL 输入、Web UI | 命令行可用，浏览器拖拽上传可用 |
| v0.4 | 06/03 16:01 | 水印平铺（含 CJK 字体嵌入）、PDF 密码、元数据 | `--watermark "机密" --password "123"` |
| v1.0 | 06/03 16:23 | Electron 桌面应用 | 桌面窗口，点击导出 PDF |
| v1.1 | 06/05 01:07 | 迁移 Tauri 框架（体积 92MB → 20MB）、Rust 后端 | 系统 WebView2 驱动，内存降到 44MB |
| v1.1.1 | 06/05 02:35 | 捆绑 Node.js 便携版、隐藏控制台、修复 Web 端渲染 | 解压即用，无需安装任何依赖 |

> 注：v0.1–v0.3 的代码在早期快速迭代中被合并提交（squash），git 历史中首个 commit 已是 v0.4。这段时间的实际产出是 CLI 核心链路 + 批量导出 + 基础 Web UI。

v0.1 从零到第一个可用的 PDF 大约 2 小时。之后的每个版本迭代在 1-3 小时之间。最大的一次改动是 v1.0 Electron → v1.1 Tauri 迁移，涉及框架替换、Rust 学习、IPC 机制重写，约 6 小时——这个决策在第 3 节详细展开。

整个过程的节奏是：**先让核心链路跑通（2h），然后每次只加一个功能，确保上一个版本稳定后再进下一步**。没有"先设计完美架构再动手"的阶段——因为一开始我连这个工具最终会长什么样都不知道。

目前项目还没有自动化测试，这是后续改进方向之一。

---

## 3. 关键技术与决策

三个有代表性的技术选择，每个都有实测对比，不是看文档决定的。

### 决策 1：渲染引擎选型

我把自己那份用了 Flex 和 Grid 布局的简历 HTML，分别喂给三个候选方案，对比输出：

| 方案 | Flex 布局 | Grid 布局 | 圆角/阴影 | 中文 | JS 支持 | 结论 |
|------|------|------|------|------|------|------|
| Puppeteer (Chromium) | ✅ | ✅ | ✅ | ✅ | ✅ | 选用 |
| wkhtmltopdf | ❌ 坍塌 | ❌ 坍塌 | ✅ | ✅ | ❌ | 淘汰 |
| WeasyPrint | ✅ | ⚠ 部分 | ❌ 丢失 | ✅ | ❌ | 淘汰 |

Puppeteer 是唯一一个排版和浏览器完全一致的方案。代价是依赖 Chromium 浏览器，但用户的电脑大概率已经装了 Chrome 或 Edge——实际上 `puppeteer-core` 可以直接复用系统已有的浏览器（代码中的 `findBrowser()` 自动扫描标准安装路径），不需要额外下载。另外，导出时默认使用 `deviceScaleFactor: 2` 以 2x 分辨率渲染，保证文字边缘足够锐利。

验证方式很简单：三行命令，看输出。不需要看文档对比表，实际跑一次就有答案了。

### 决策 2：桌面框架——Electron → Tauri 迁移

第一版桌面应用用 Electron 做的，3 小时搭完。安装包打出来一看：92MB。实际运行还暴露了一个前端路径配置问题导致窗口全白屏——修好之后能用了，但 92MB 的体积对于一个小工具来说不可接受。

查了对比数据，决定迁移到 Tauri：

| | Electron v1.0 | Tauri v1.1 |
|------|------|------|
| exe 大小 | 92 MB | 20 MB |
| 运行时内存 | 4 进程 / ~360 MB | 2 进程 / ~44 MB |
| 捆绑了什么 | Chromium 完整运行时 | 系统 WebView2（Windows 10+ 自带） |
| 启动速度 | ~3 秒 | ~1 秒 |

迁移花了 6 小时。不是 Tauri 本身难，是 Rust 和 Tauri v2 的文档不够完善——有两个 bug 各耗了 2-3 个小时（详见第 5 节）。但如果重来一次，我还是会先写 Electron 版本：不是因为 Electron 好，而是**人不看到糟糕的结果，就没有动力去找更好的方案**。如果我一开始就调研桌面框架，大概率会在"选 Electron 还是 Tauri"上纠结半天而一行代码没写。先用最简单的方案跑起来，看到 92MB 的结果后再去找更好的——这种节奏比"先做完美调研"更适合个人项目。

### 决策 3：后处理管道——保留 JS，不追求纯 Rust

PDF 的水印平铺、AES 加密、元数据注入——这些功能在 Node.js 生态里用 `pdf-lib-plus-encrypt` 两天就做完了。水印模块还包含一个实用细节：用正则 `/[一-鿿]/` 检测中文字符，自动切换 SimHei 字体嵌入，英文水印则用 Helvetica——中文用户不用手动配置字体路径。

如果全部用 Rust 重写，需要找等价的 PDF 操作库、处理 CJK 字体嵌入、实现加密算法。保守估计 3-5 天，而且每一步都可能遇到第 5 节那种"文档不完善 → 踩坑 → 排查"的循环。

选择了折中：保留 Node.js 依赖，但做了三件事弥补：
- 把 Node.js 便携版捆绑进安装包，用户无需自行安装
- 用 Windows `CREATE_NO_WINDOW` 标志隐藏了子进程控制台弹窗。此外，`main.rs` 的 `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` 属性在 release build 下将主进程也隐藏了控制台——双重保证"无窗口"体验
- 在 PRD 里把"Chrome 直调模式（纯 Rust，零 Node.js 依赖）"列入后续计划——不是为了好看，是确实想做，只是时间换不来

这个决策的核心逻辑是：**在"快速出产品"和"架构纯粹性"之间，我选前者**。架构的纯粹性可以重构，但用户的需求不会等你重构完。

---

## 4. 产品形态：三种入口，一套引擎

做完了 CLI（给自己用）之后，想到两个问题：如果别人要用，他们可能不会命令行；如果我自己偶尔在另一台电脑上用，调 CLI 也不方便。

于是又多做了两种入口——但核心引擎只有一份。

```
┌──────────┐  ┌──────────┐  ┌──────────────┐
│ CLI 命令行 │  │ Web UI   │  │ 桌面应用(Tauri)│
│ 开发者/脚本│  │ 浏览器拖拽│  │ 双击即用      │
└────┬─────┘  └────┬─────┘  └──────┬───────┘
     │             │              │
     └─────────────┼──────────────┘
                   │
          ┌────────▼────────┐
          │  html2pdf-lib   │  ~150 行核心引擎
          │  (Puppeteer)    │
          └────────┬────────┘
                   │
     ┌─────────────┼─────────────┐
     ▼             ▼             ▼
  Chromium     pdf-lib       Node.js
  (页面渲染)   (后处理)       (便携版)
```

三种入口共享同一套 `html2pdf-lib.js`（约 150 行），各自只是调用方式不同。CLI 用 Commander 解析参数，Web UI 用 Express + Multer 处理上传，桌面应用通过 Tauri Rust 后端 spawn Node.js 子进程执行导出——每次导出启动独立进程，天然避免了长驻内存问题，代价是冷启动约 1-2 秒的延迟。

做三层入口不是为了"显得全面"——是三种使用场景确实不同：
- **CLI**：我自己日常用，可以写脚本批量导出
- **Web UI**：偶尔在没装开发工具的电脑上，打开浏览器就能用
- **桌面应用**：给别人用，双击打开，拖文件进去，点导出

---

## 5. 踩坑与调试：三个真实 Bug

这三个 Bug 不是编的，每一个在项目 commit 记录里都能找到对应的修复。

### Bug 1：Windows 文件对话框不弹出（排查 + 修复 ~3h）

**现象**：Tauri 桌面端点击「导出 PDF」，界面显示 loading，但文件选择框从未出现。过几秒报错「取消选择文件」。

**排查**：先确认前端 invoke 调用已发出，然后在 Rust 函数入口加了 `eprintln!`——发现日志没输出。说明 Rust 函数根本没收到调用。

让 Agent 审查全链路代码（`tauri.conf.json` → `lib.rs` → `main.rs` → `capabilities` → 前端 HTML），它在一分钟内指出了问题：`blocking_pick_file()` 在 `async fn` 中被调用，Windows 文件对话框要求调用线程必须初始化 COM STA 模式，而 Tokio 异步线程池没有初始化 COM。

**修复**：把 `#[tauri::command] async fn pick_and_export(...)` 改成 `#[tauri::command] fn pick_and_export(...)`（去掉 async）。Tauri 自动将同步命令调度到有 COM 支持的线程上。改一行代码，前面的排查花了近 3 小时——其中我单人排查约 1.5 小时换了两个方向都没中，最后靠 Agent 审查一次性定位。

### Bug 2：Web 端导出 PDF 显示 HTML 源代码（~15min）

**现象**：Web UI 上传 HTML 文件后，PDF 里显示的是 `<html><head><style>...` 源代码，不是渲染后的页面。

**排查**：用同样的文件在 CLI 测试，输出正常，排除渲染引擎问题。对比 Web 和 CLI 的唯一差异：Web 的文件经过了 multer 中间件上传处理。

**根因**：multer 把上传文件保存为无后缀名的临时文件（如 `uploads/a1b2c3d4`），Chrome 通过 `file://` 加载时无法识别文件类型，按 `text/plain` 渲染。**修复**：在传给 Puppeteer 之前把临时文件重命名为加 `.html` 后缀。

### Bug 3：Tauri 文件拖拽无响应（~2h）

**现象**：从资源管理器拖 HTML 文件到桌面窗口，什么都没发生。但点击拖拽区弹出的文件选择框是正常的。

**排查**：前端 JS 的 `drop` 事件里检查 `e.dataTransfer.files[0].path`——返回 `undefined`。Tauri 的 WebView2 不像 Electron 那样给 File 对象自动加 `path` 属性。

试了三种方案：
1. JS 层直接读 `dataTransfer` — 拿到 File 对象但没有路径
2. Tauri dialog plugin 的 JS 端 API — 权限配置不生效
3. Rust 层 `on_window_event` 监听 `WindowEvent::DragDrop`，然后 `emit` 到前端 — 成功

**修复**：在 Rust 的 `setup` 闭包注册窗口事件监听，检测到 `DragDropEvent::Drop` 时把文件路径通过 IPC 事件发给前端。前端用 `window.__TAURI__.event.listen('file-dropped')` 接收。

这三个 Bug 有一个共同模式：**问题本身不复杂，但根因往往在跨层交互的边界上**——Rust 的线程模型和 Windows COM、multer 的文件命名和 Chrome 的 MIME 检测、WebView2 的安全模型和文件 API。这些边界问题在文档里通常不会写，只能靠实际踩坑。

另外，Web 端还遇到了一个实用的兼容性问题：用户的 Internet Download Manager（IDM）会在浏览器下载 PDF 时自动拦截，导致文件无法正常保存。这跟代码逻辑无关，但影响实际使用。修复方式是在 Express 响应头中加了 `Cache-Control: no-cache, no-store`、`X-Content-Type-Options: nosniff` 和 `Content-Transfer-Encoding: binary`——这些头告诉下载管理器"这不是一个需要拦截的文件"。

---

## 6. AI 的使用方式

这个项目里 AI 不是"一键生成"，而是在几个具体环节充当了杠杆。

**PRD 和初版代码**：我先描述了核心需求（HTML 导出 PDF、长页面单页、矢量化），AI 产出 PRD 和 CLI 的 v0 代码。我没有直接接受——如果接受了，渲染引擎会是 wkhtmltopdf（AI 推荐最轻量的方案），但实测我的简历排版会直接坍掉。最终选 Puppeteer 是我亲手对比三个方案输出后做的决定。AI 提供的是一个可以立刻被批评和修改的起点，这比从零写快很多。

**Agent 代码审查**：第 5 节 Bug 1（文件对话框不弹出）是 Agent 的审查直接定位了问题——`blocking_pick_file()` 在 `async fn` 中的 COM 线程冲突。我单独排查了约 1.5 小时没找到根因，Agent 在一分钟内并行检查了五层代码链路。这不是 AI 比我更懂 Rust——是我在逐层排查时有盲区，而它能同时看到所有层——包括一个我自己知道但没联想到的知识点（Windows COM 需要 STA 线程）。

**迭代节奏**：大部分 Bug 的闭环是"描述现象 → AI 给方案 → 我验证 → 不行就补充描述 → AI 改方案"。短的 10 分钟，长的 3 小时。比"Google → 看文档 → 试 → 失败 → 换关键词再搜"快一个数量级。关键不是 AI 每次都对——经常不对——而是反馈循环极短，不对就立刻换方向。

**我没有让 AI 做的事**：技术选型的最终决定（三个方案的实测对比是我亲手做的）、产品形态的设计（CLI/Web/桌面三种入口是我决定的）、Electron → Tauri 的迁移判断（AI 可以列对比表，但它没看到 92MB 那个结果，不知道"体积是真实问题"）。

---

## 7. 反思与后续

### 最正确的决定

先按自己的实际需求做 MVP（CLI 单页导出），而不是一开始就画大图。MVP 2 小时跑通，验证了核心链路可行，后续功能全是在这个基础上叠加。如果一开始就设计"支持三种入口、六种后处理、跨平台打包"的架构，大概率花两天画图然后不了了之。

### 最昂贵的学习

Electron → Tauri 的迁移，6 小时。如果一开始就调研桌面框架选型可以省掉。但换个角度：如果不先用 Electron 做出 92MB 的成品，亲眼看到那个体积，我不会有足够的动力去啃 Rust 和 Tauri。有些事情不亲自踩一遍，光看对比表是不会信的。

### 后续打算

- **Chrome 直调模式**：Rust 直接调系统 Chrome `--print-to-pdf`，彻底移除 Node.js 依赖。不做不是因为做不到，是水印/密码/元数据的后处理在 JS 生态已成熟，Rust 重写成本（预估 3-5 天）在当前阶段不划算。列入 PRD 后续计划
- **CI/CD 自动构建**：每次 push 自动打 Tauri 安装包并上传到 GitHub Release。目前仍是手动构建 + 上传
- **自动化测试**：当前缺少渲染一致性的自动化对比和单元测试覆盖
- **macOS 支持**：Tauri 框架本身跨平台，但只在 Windows 上测试过

### 如果加入贵司的游戏团队

加入凉屋游戏后，我想把这种"从真问题出发、快速验证、AI 加速"的工作方式带到运营一线。从写一个小工具到策划一次玩家活动，底层的做事逻辑是一样的：**发现真实问题 → 最小成本验证 → 迭代优化 → 让结果说话**。

---

*项目时间线：2026-06-03 至 2026-06-05，单人完成全部工作。从一份真实需求出发，最终产出 CLI 工具、Web UI、Tauri 桌面应用三种形态。活跃开发时间约 18-22 小时。GitHub 仓库：[github.com/Raaain9527/html-pdf](https://github.com/Raaain9527/html-pdf)*
