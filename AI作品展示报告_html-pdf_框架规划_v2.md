# 《HTML → PDF 矢量导出工具》— AI 作品展示报告 · 框架规划 v2.0

## v2.0 变更说明（相对于 v1.0）

基于投递岗位调整，本版做以下结构性变更：

| 变更 | 原因 |
|------|------|
| **叙事主线重新定位**：以"自驱解决问题 + AI 杠杆"为核心，技术细节作为能力证据而非主角 | v1.0 读起来像全栈工程师的作品展示——但投的岗位是游戏运营，面试官不需要理解 Rust 的线程模型 |
| **岗位适配**：投递公司为凉屋游戏，岗位为游戏运营实习生 | v1.0 没有连接具体岗位——面试官不知道"这份报告对应我的什么需求" |
| **新增第 7 节"能力迁移"** | 核心命题：如何把"写了一个工具"翻译成运营岗位需要的能力——流程自动化、数据思维、AI 生产工具使用、自驱闭环 |
| **AI 使用方式重新表述** | v1.0 聚焦"AI 帮我写代码"，v2.0 聚焦"AI 帮我从零到一搞定一个需求"——后者是运营岗需要的叙事 |
| **技术内容精简但不删除** | 保留时间线表、架构简图、2 个代表性 Bug；删去 COM STA / WebView2 API / async fn 粒度等技术术语 |
| **保留 v1.0 作为附件** | v1.0 保留在仓库中，标注为技术版，供面试官深入了解技术细节 |

---

## 报告定位（调整后）

投递附件 / 作品集，篇幅 ~2,000 字，适合 3-4 页阅读。面向凉屋游戏运营实习生岗位。

**核心叙事（重新定位）**：

> 投递过程中我发现自己的 HTML 简历导出为 PDF 后文字变成不可选中的图片，影响 ATS 解析。市面上没有现成的工具能同时满足"矢量化 + 长页面单页"。于是我借助 AI，用了两天时间从零到一完成了这个工具——不是为了炫技，是我真的需要用它。这段经历背后是我认为对游戏运营同样重要的能力：**从真实问题出发、不被技术栈限制、用 AI 放大个人产能、把一次性需求变成可复用产出**。

叙事主线：**自驱解决问题 + AI 杠杆**。技术细节是证据，不是主角。

---

## 结构（共 7 节）

### 1. 从一份简历开始的工具（~250 字）

- 场景：投凉屋游戏的简历是 HTML 写的，导出 PDF 后 ATS 无法解析
- 三个具体问题：文字栅格化、长页面截断、排版偏移
- 市面方案试了一圈都不行——这是"我需要用"而非"我要练技术"
- 核心判断：这个痛点可能别人也有，但关键是**我选择动手解决而不是凑合用**

### 2. 两天做了什么（~200 字 + 版本表）

- 精简版时间线，不纠结具体时间戳，强调迭代节奏
- 每个版本一句话——"加了这个功能是因为这个场景需要它"

| 版本 | 做了什么 | 为什么加它 |
|------|------|------|
| v0.1 | CLI 单页矢量导出 | 核心需求：我需要导出简历 |
| v0.2 | Web UI + 批量 + URL | 换电脑也能用，一次导出多份简历 |
| v0.4 | 水印 / 密码 / 元数据 | 有些场景需要标注"内部文件" |
| v1.1 | Tauri 桌面应用 | 给别人用——双击打开，拖文件进去就行 |

- 节奏说明：MVP 2 小时跑通，每个版本 1-3 小时，总共 ~20 小时

### 3. 三个关键选择（~300 字）

选三个决策，每个强调"判断"而非"技术"：

**为什么不凑合用浏览器另存为**：实测三份简历，Chrome 另存为的文字全部栅格化、三屏简历被切成三页。在线工具有水印和隐私顾虑。Puppeteer 是唯一一个排版和浏览器一致的方案——不是看文档选的，是把简历分别喂给三个方案对比出来的。

**为什么做了三种形态**：CLI 给自己、Web UI 给偶尔用的场景、桌面应用给完全不懂命令行的人。三种入口共享同一套核心引擎（~150 行）——不是为了"做得全面"，是真的各有各的用途。

**为什么保留 Node.js 而不是全用 Rust**：水印、密码、元数据这些功能在 JS 生态里已经成熟。Rust 重写预估 3-5 天。在"快速出产品"和"架构纯粹"之间选了前者——运营工作中同样的逻辑：先上线验证效果，再考虑优化。

### 4. 踩过的坑（~250 字，选 2 个有代表性的）

从原第五节选 2 个 Bug，每个用"什么问题 → 怎么发现 → 怎么修"的三段式，去掉技术术语：

**Bug 1：文件对话框弹不出来**。现象是点击导出后一直 loading，排查半天发现是 Rust 的异步线程和 Windows 文件对话框不兼容——根因和我学的专业（GIS 空间分析）毫无关系，但排查的思路是一样的：逐层假设、验证、缩小范围。最后借助 AI 审查一分钟定位了问题。

**Bug 2：Web 端导出的 PDF 是源代码**。根因是上传的文件没有后缀名，Chrome 不认识。修复就一行代码——但这个问题暴露了一个常见的盲区：**工具链中的一个环节做了你没预料到的处理**（multer 会自动去掉文件后缀）。运营工作中同样需要这种"不被表面现象迷惑，追到上游找根因"的习惯。

- 关键认知：Bug 往往在跨层交互的边界上——这些地方文档不会写，只能实际踩

### 5. AI 的使用方式（~300 字）

重新聚焦于运营岗相关的 AI 应用场景：

- **PRD 生成 → 快速启动**：描述了需求，AI 产出 PRD 和代码框架。人做了关键修正（渲染引擎从 AI 推荐的 wkhtmltopdf 换成了实测效果更好的 Puppeteer）
- **代码审查 → 定位问题**：排查一个多小时没找到的问题，AI 在一分钟内全链路审查后定位——不是 AI 比我聪明，是它能同时看五层代码而我有盲区
- **迭代节奏**："描述现象 → AI 给方案 → 我验证 → 不行再描述"的闭环比 Google 查文档快一个数量级
- **AI 没做的事**：最终的技术选型、产品形态设计、质量判断——这些仍然是人做的

关键论点：**AI 不会取代运营——但能把"能独立解决问题"和"不能"之间的差距从 2x 拉到 10x。**

### 6. 质量保障（~150 字）

新增节：

- 用 8 份不同结构的 HTML 做了回归测试（Flex 多栏、Grid 瀑布流、中文长文、含图表页面等）
- 项目 commit 记录完整可追溯
- 发布了 3 个 GitHub Release，每个附带安装包和便携版 zip
- 坦承：目前没有自动化测试，这是后续改进方向

### 7. 能力迁移：从写工具到做运营（~350 字）

核心节。不做"我学过什么"的列表，而是"这段经历对应了运营岗的什么能力"：

| 项目中的实践 | 对运营工作的意义 |
|------|------|
| 从自身痛点到完整产品，两天从零到一 | 运营需要发现问题的敏感度和解决问题的主动性——不是等需求，是找需求 |
| 三个方案实测对比后选 Puppeteer | 运营决策需要数据支撑，不是凭感觉——AB 测试、竞品分析的底层逻辑相通 |
| CLI / Web / 桌面三种形态 | 运营工作要考虑不同用户场景——公告写给核心玩家和推给泛用户是不同的语言 |
| 6 小时 Electron → Tauri 迁移 | 发现一个方案不好，有勇气推倒重来——活动效果不好时也需要果断调整而非硬撑 |
| 8 份 HTML 回归测试 | 任何产出物都需要质量验证——运营活动上线前要想清楚"怎么算成功"和"哪里可能出错" |
| 把 AI 用到实际工作流里 | 已经证明能把 AI 变成生产力工具——不只是"用过 AI"，是"用 AI 交付过完整产品" |

**收尾（50-80 字）**：加入凉屋游戏后，我想把这种"从问题出发、快速验证、AI 加速"的工作方式带到运营一线——从小工具到运营活动，底层的做事逻辑是一样的。

---

## 总篇幅

~2,000 字正文 + 2 个表格。适合 3-4 页阅读。

---

*规划版本：v2.0 | 2026-06-05 | 基于岗位调整重新定位，v1.0 保留在仓库作为技术参考*
