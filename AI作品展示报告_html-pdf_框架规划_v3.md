# 《HTML → PDF 矢量导出工具》— AI 作品展示报告 · 框架规划 v3.0

## v3.0 变更说明（相对于 v2.0）

| 变更 | 原因 |
|------|------|
| **删除所有版本的"8 份 HTML 回归测试"编造内容** | 两份 Agent 审查均发现该项目无任何测试 HTML 文件——v1 第 2 节和 v2 第 6 节均包含此编造。v3 全部删除 |
| **第 3 节从技术名词改为决策方法论** | Agent 2 指出原标题（"渲染引擎选型""Electron→Tauri迁移""后处理管道"）会让运营面试官直接跳过。改为"不只看文档，实测后再选""发现不对就推倒重来""先交付再优化"——三个判断，不是三个技术选型 |
| **第 4 节和第 5 节去重** | 两份审查均指出 Bug 1 的 Agent 审查故事在第 4 和第 5 节各讲一遍。v3 在第 4 节只留现象→排查→修复，Agent 部分缩减为一句话前向引用"详见第 5 节" |
| **恢复 v1 的第 5 节「AI 的使用方式」和第 6 节「反思」** | 这是 v1 中两份 Agent 审查评分最高的章节，真实且具体。v2 丢失了这部分内容 |
| **第 6 节标题从"反思"改为"如果重来一次"** | Agent 2 指出"反思"像述职模板 |
| **第 7 节能力映射表重写** | Agent 2 指出两行弱行（第 1 行逻辑跨度太大、第 5 行本质是"我 AI 很熟"）。v3 全部重写，每行都对应具体的运营工作场景 |
| **收尾段加游戏运营假设场景** | Agent 2 指出原收尾"发现问题→验证→迭代→让结果说话"可套任何岗位。v3 加了凉屋运营的具体假设场景（活动参与率低→排查入口 vs 奖励→最小成本验证） |
| **修正事实性偏差** | Bug 1 "改一行代码"→"把异步函数改为同步函数"；"Agent 一分钟"→"几分钟内"；删除"发布了 3 个 GitHub Release"（无法验证）；删除 v1 第 2 节的编造内容 |

---

## 报告定位

投递附件 / 作品集，篇幅 ~2,200-2,500 字，适合 3-4 页阅读。面向凉屋游戏运营实习生岗位。

**核心叙事**：

> 投递凉屋游戏时，我发现自己的 HTML 简历导出 PDF 后文字变成了不可选中的图片——ATS 简历解析系统读不到任何信息。市面上没有现成的工具能同时做到"矢量文字 + 长页面不分页"。我没有凑合，而是借助 AI，用了两天从零到一写了这个工具。这段经历背后是我认为对游戏运营同样重要的能力：从真实问题出发、用最小成本验证、敢于推翻自己的方案、把 AI 当作杠杆而非替代品。

---

## 结构（共 7 节）

### 1. 从一份简历开始的工具（~250 字）

- 场景：投凉屋游戏的简历是 HTML 写的，导出 PDF 后 ATS 无法解析
- 三个具体问题：文字栅格化、长页面截断、排版偏移
- 市面方案试了一圈都不行——这是"我需要用"而非"我要练技术"
- 核心判断：这个痛点可能别人也有，但关键是**我选择动手解决而不是凑合用**

### 2. 两天做了什么（~200 字 + 版本表）

精简时间线，每个版本一句话——"加了这个功能是因为这个场景需要它"：

| 版本 | 做了什么 | 为什么加它 |
|------|------|------|
| v0.1–v0.3 | CLI 核心导出、批量处理、Web UI | 我需要导出简历；换电脑也能用；一次导出多份 |
| v0.4 | 水印平铺、PDF 密码、元数据 | 有些场景需要标注"内部文件"，有些文件需要保护 |
| v1.0–v1.1 | 桌面应用（Electron → Tauri 迁移） | 给别人用——双击打开就行。第一版 92MB，迁移后降到 20MB |
| v1.1.1 | 捆绑 Node.js 便携版、修复各种边界问题 | 让完全不懂命令行的人解压即用 |

- MVP 2 小时跑通，每个版本 1-3 小时，总共 ~20 小时
- 三种形态共享核心引擎的架构简图（CLI / Web / Desktop → html2pdf-lib）

### 3. 三个判断（~350 字）

标题从技术名词改为决策方法论：

**不只看文档，实测后再选**（原"渲染引擎选型"）：
- 实测对比表保留（Puppeteer vs wkhtmltopdf vs WeasyPrint）
- 核心不是"Puppeteer 赢了"，是"AI 推荐了 wkhtmltopdf，实测发现不对，人做了纠正"
- 结论句：工具能给你信息，但判断信息对不对只能人来做

**发现不对就推倒重来**（原"Electron→Tauri 迁移"）：
- 体积对比表保留（92MB → 20MB）
- 核心不是"Tauri 更好"，是"先看到 92MB 成品才意识到体积是真实问题 → 才有动力去换方案"
- 结论句：有些坑光看对比表不会信，亲自踩了才信

**先交付再优化**（原"后处理管道保留 JS"）：
- 保留 Node.js 而非全部 Rust 重写——"两天能做完 vs 三五天"的决策
- 水印 CJK 自动检测作为细节点缀
- 结论句：先上线验证效果，再考虑优化——运营同理

### 4. 踩过的坑（~250 字，2 个 Bug）

两个 Bug，每个用"现象 → 排查 → 修复"结构。去技术术语，保留排查思路。

**Bug 1：文件对话框弹不出来**：
- 现象：点击导出后 loading，文件选择框不出现
- 排查：确认前端调用已发出 → 后台加日志 → 日志没输出 → 函数没收到调用 → 借助 AI 定位（详见第 5 节）
- 修复：把异步函数改为同步函数，让系统调度到正确的线程
- Agent 审查的故事缩减为一句话 + 前向引用"详见第 5 节"

**Bug 2：Web 端导出显示源代码**：
- 现象：PDF 里是 HTML 源码，不是渲染页面
- 排查：CLI 测同一文件正常 → 排除引擎问题 → 对比流程差异 → 定位到上传组件自动去掉了文件后缀
- 修复：渲染前重命名加 `.html`
- 核心认知：工具链某环节做了你没预料到的处理——运营中分析数据时同理

### 5. AI 的使用方式（~350 字，恢复自 v1.0）

保持 v1.0 的 4 个子点，调整措辞避开运营面试官不关心的技术细节：

- **PRD 和初版代码**：AI 产出 PRD + v0 代码。AI 推荐 wkhtmltopdf（最轻量），实测简历排版坍塌，人决定换 Puppeteer。AI 提供可立刻批评修改的起点——但判断"推荐对不对"需要人亲手验证
- **代码审查定位问题**：Bug 1 排查一个多小时没定位，AI 在几分钟内并行检查五层代码找到根因。这是 Bug 1 Agent 故事的完整版（第 4 节不再展开）
- **迭代节奏**："描述现象 → AI 给方案 → 验证 → 不行再描述"的闭环比传统搜索→文档→试错快一个数量级
- **AI 没做的事**：技术选型实测、产品形态设计、Electron→Tauri 迁移判断

### 6. 如果重来一次（~300 字，恢复自 v1.0 并扩充）

标题从"反思"改为"如果重来一次"（Agent 2 建议）。

- **最正确的决定**：先做 MVP，2 小时跑通核心链路。不做完美架构，先验证假设
- **最昂贵的学习**：Electron → Tauri 的 6 小时。如果不先用 Electron 做出 92MB 成品，不会有动力换方案
- **两件事的串联**（新增）：合在一起说明同一套做事方式——先用最小成本验证核心假设，验证通过了再追加投资，发现不对就果断改
- **后续打算**（3 项）：CI/CD 自动构建、自动化测试、macOS 支持

### 7. 能力迁移：从写工具到做运营（~400 字，重写）

能力映射表全部重写（Agent 2 指出两条弱行），每行对应具体运营场景：

| 项目中的实践 | 对运营工作的意义 |
|------|------|
| 两天内把一个想法变成三种形态的完整产品并发布 | 运营同样需要项目闭环能力——发现玩家流失信号→提出假设→设计干预→看数据反馈→迭代优化。完整的"从零到一"周期 |
| 三个方案实测对比后选 Puppeteer——不是看文档，是看输出 | 运营决策需要依据——活动方案 A 还是 B，应该测过再定，不是凭感觉。数据比文档可靠 |
| 做了 CLI / Web / 桌面三种形态 | 运营内容要考虑受众——核心玩家的公告和泛用户的推送，是两种语言、两种渠道 |
| 发现第一版 92MB 后果断迁移框架 | 活动效果不好时，敢承认"方案不对"并调整，比硬撑更划算 |
| 把 AI 用到实际工作流里——不只"用过"，是交付了完整产品 | 运营同样需要"AI 当杠杆，人做判断"——比如 AI 生成活动文案或分析社区舆论，人根据玩家画像和品牌调性做筛选和决策 |

**收尾**（加游戏运营假设场景）：加入凉屋后面对"活动参与率远低于预期"，先问"入口曝光不够还是奖励吸引力不对"→ 分别最小成本验证 → 而非直接推翻方案。从写小工具到策划活动，底层逻辑一样：发现真实问题 → 最小成本验证 → 迭代优化 → 让结果说话。

---

## 总篇幅

~2,200-2,500 字正文 + 3 个表格 + 1 张架构简图。适合 3-4 页阅读。

---

## 与 v1.0 / v2.0 的关系

- **v1.0**（技术版，保留）：面向开发岗。含全部 3 个 Bug、技术选型详述、开发向能力映射。**注意：v1 第 2 节也包含"8 份 HTML 回归测试"编造，需同步清理**
- **v2.0**（运营版，废弃）：第 6 节编造"质量保障"、AI 使用方式过度精简、能力映射有两行弱行
- **v3.0**（运营版，当前）：以 v1 真实章节为骨架、以 v2 运营定位为方向、根据双 Agent 审查意见重构

---

*规划版本：v3.0 | 2026-06-05 | 基于双 Agent 交叉审查（事实核查 + 结构/受众）修订*
