# 从一份简历出发——两天，借助 AI 从零到一完成一个工具

> **投递岗位**：凉屋游戏 · 实习生
>
> **GitHub**：[github.com/Raaain9527/html-pdf](https://github.com/Raaain9527/html-pdf)
>
> **背景**： 2026 年 6 月求职季，我需要频繁将 HTML 简历导出为 PDF 投递。但浏览器「另存为 PDF」产出的栅格图文字无法被 ATS（简历解析系统）识别，长页面还会被截断。市面上没有工具同时满足"矢量化 + 长页面单页 + 中文水印"——于是我自己写了一个。我没有凑合，而是借助 AI，用了两天时间从零到一写了这个工具。
>
> **投递作品说明**：这不是一个"技术练习"项目——它来自我投简历时遇到的真实问题。工具覆盖 CLI 命令行、Web UI、桌面应用三种形态。以下记录的是我如何发现问题、做判断、借助 AI 放大效率、以及这段经历和游戏运营工作的关联。

---

## 目录

1. [从一份简历开始的工具](#1-从一份简历开始的工具)
2. [两天做了什么](#2-两天做了什么)
3. [三个判断](#3-三个判断)
4. [踩过的坑](#4-踩过的坑)
5. [AI 的使用方式](#5-ai-的使用方式)
6. [如果重来一次](#6-如果重来一次)
7. [能力迁移：从写工具到做运营](#7-能力迁移从写工具到做运营)

---

## 1. 从一份简历开始的工具

投凉屋游戏的简历是用 HTML + CSS 写的，排版用了 Flex 和 Grid 布局。在浏览器里一切正常，但 Chrome「另存为 PDF」之后：

1. **文字变成了图片**。中文文本无法被选中、复制、搜索——ATS 简历解析系统读不到我的信息，简历等于白投
2. **长页面被截断**。三屏长的内容被切成了三页 PDF，中间还有空白间隙
3. **排版偏移**。浏览器里居中对齐的模块，到了 PDF 里偏了 20px

试了一圈市面上的方案：**wkhtmltopdf**（2016 年后没更新过，Flex 布局直接坍塌）、**在线转换工具**（限量、加水印、需要上传到第三方服务器）、**浏览器打印对话框**（分页线完全不可控，标题刚好卡在切口上）。

这些方案有一个共同问题——它们不是"我需要的工具"。我是一个求职者，我只想把一份 HTML 干净地导出为矢量 PDF。需求简单到一句话就能说清楚，但就是没有现成的工具能做好。

于是我自己写了。不是"我要练技术"，是"我真的需要这个东西，现在就要用"。

---

## 2. 两天做了什么

项目在 6 月 3 日至 5 日间完成核心开发，活跃工作时间约 18-22 小时。每个版本的迭代都是因为"这个场景真的需要它"：

| 版本 | 做了什么 | 为什么加它 |
|------|------|------|
| v0.1–v0.3 | CLI 核心导出、批量处理、Web UI | 我需要导出简历；换电脑也能用；一次导出多份 |
| v0.4 | 水印平铺、PDF 密码、元数据 | 有些场景需要标注"内部文件"，有些文件需要保护 |
| v1.0–v1.1 | 桌面应用（Electron → Tauri 迁移） | 给别人用——双击打开就行。第一版 92MB，迁移后降到 20MB |
| v1.1.1 | 捆绑 Node.js 便携版、修复各种边界问题 | 让完全不懂命令行的人解压即用 |

MVP 从零到第一个可用的 PDF 大约 2 小时。之后的每个版本迭代在 1-3 小时之间。最大的一次改动是桌面框架从 Electron 迁到 Tauri——这个判断在第 3 节展开。

整个过程的节奏是：**先让核心链路跑通，然后每次只加一个功能，确保上一个版本稳定后再进下一步**。

三种形态共享同一套核心引擎（约 150 行），各自只是入口不同：

```
CLI 命令行         Web UI           桌面应用 (Tauri)
(开发者/脚本)      (浏览器拖拽)      (双击即用)
     │                 │                 │
     └─────────────────┼─────────────────┘
                       │
               html2pdf-lib (~150行)
               (Puppeteer 渲染引擎)
```

做三种形态不是因为"做得全面好看"——是真实使用场景不一样：CLI 给自己日常用，Web UI 给偶尔换电脑时用，桌面应用给不碰命令行的朋友用。

---

## 3. 三个判断

### 不只看文档，实测后再选

我把自己的简历 HTML 分别喂给三个候选方案，实际对比输出：

| 方案 | Flex | Grid | 圆角/阴影 | 中文 | 结论 |
|------|------|------|------|------|------|
| Puppeteer (Chromium) | ✅ | ✅ | ✅ | ✅ | **选用** |
| wkhtmltopdf | ❌ 坍塌 | ❌ 坍塌 | ✅ | ✅ | 淘汰 |
| WeasyPrint | ✅ | ⚠ 部分 | ❌ 丢失 | ✅ | 淘汰 |

验证方式不是看文档对比表——是三个方案各跑一次，看输出。只有 Puppeteer 的排版和浏览器完全一致。

AI 最初推荐的是 wkhtmltopdf，因为"最轻量、命令行友好"。如果我直接接受，产出物排版就是坏的。**工具能给你信息，但判断信息对不对、适不适合自己的场景，只能人来做。**

### 发现不对就推倒重来

第一版桌面应用用 Electron 做的，3 小时搭完。安装包打出来一看：92MB。

对于一个"导出 PDF"的小工具，这个体积明显不对。查了对比数据后迁到 Tauri：

| | Electron 第一版 | Tauri 迁移后 |
|------|------|------|
| exe 大小 | 92 MB | 20 MB |
| 运行时占用 | 4 个进程 | 2 个进程 |
| 捆绑了什么 | 完整 Chromium | 系统自带的 WebView2 |

迁移花了 6 小时。如果一开始就做框架调研可以省掉这段时间。但如果重来一次我还是会先做 Electron——因为不先看到 92MB 的成品摆在那里，我不会意识到"体积是真实问题"，也就不会有动力去啃一份全新的 Rust 框架文档。**有些事情光看对比表不会信，亲自踩了才信。**

### 先交付再优化

水印、密码、元数据这些功能，在 JS 生态里两天就做完了。水印模块还做了一个实用细节：自动检测水印文字是否含中文，有中文就嵌入系统黑体，没有就用标准英文 Helvetica——用户不用手动配置字体。

如果全部用 Rust 重写这些后处理，预估 3-5 天。在"快速出产品"和"架构纯粹"之间，选了前者。**运营工作的逻辑一样：先上线验证效果，再考虑优化——而不是在方案阶段追求完美。**

---

## 4. 踩过的坑

### Bug 1：文件对话框弹不出来

点击「导出 PDF」，界面显示 loading，但 Windows 的文件选择框就是不出现。过几秒报错"取消选择"。

先确认前端调用已发出 → 在后台函数入口加日志 → 日志没输出，说明函数根本没收到调用。借助 AI 审查了整条代码链路后定位到问题：文件对话框需要在特定类型的线程上运行，而当时代码跑在了错误的线程上。

修复方式是把异步函数改为同步函数，让系统自动调度到正确的线程。排查花了近三小时——中间试了几个方向都没中。关于 AI 在这个排查过程中具体怎么帮到我的，见第 5 节。

### Bug 2：Web 端导出的 PDF 是源代码

上传 HTML 文件后导出，PDF 里显示的是 `<html><head><style>...` 的源代码，不是渲染后的页面。

用 CLI 测试同样的文件——正常。说明渲染引擎本身没问题。对比 Web 和 CLI 的流程差异：Web 端多了一步文件上传。查下去发现，上传组件（multer）会把文件存成一个没有后缀名的临时文件名（如 `a1b2c3d4`），Chrome 打开时不知道它是 HTML，就按纯文本渲染了。

修复：在渲染之前重命名加 `.html` 后缀。这个问题暴露了一个常见盲区：**工具链中的某个环节做了你没预料到的处理**。运营工作中——比如分析玩家行为数据时——同样需要"不被表面现象迷惑，追到上游找根因"的习惯。

---

## 5. AI 的使用方式

这个项目里 AI 不是"一键生成"——它在几个具体环节放大了我的效率。

**PRD 和初版代码**。我先描述了核心需求，AI 产出 PRD 和 CLI 的 v0 代码。AI 推荐用 wkhtmltopdf（最轻量），但实测我的简历排版会坍掉——最终选了 Puppeteer。AI 提供的是一个可以被立刻批评和修改的起点，这比从零写快很多。**但判断"这个推荐对不对"，需要人亲手验证。**

**代码审查定位问题**。第 4 节的 Bug 1 排查了一个多小时没找到根因，换了几个方向都不对。AI 在几分钟内并行检查了五层代码（前端调用 → IPC 传输 → 命令注册 → 函数签名 → 线程模型），指出文件对话框的线程兼容性问题是根因——这是我单独排查时没有覆盖到的盲区。不是 AI 比我更懂，是它能同时看到所有层，而我在逐层排查时有遗漏。

**迭代节奏**。大部分 Bug 的闭环是"描述现象 → AI 给方案 → 我验证 → 不行再描述 → AI 改方案"。短的十分钟，长的几小时。比"Google → 看文档 → 试 → 失败 → 换关键词再搜"快一个数量级。关键不是 AI 每次都对——经常不对——而是反馈循环极短，错了立刻换方向。

**AI 没做的事**。最终的技术选型是我实测对比三个方案后决定的、产品形态（三种入口）是我根据实际使用场景设计的、Electron → Tauri 的迁移判断是我看到 92MB 成品后做出的。AI 可以列对比表，但它不知道一个小工具 92MB 对用户意味着什么。

---

## 6. 如果重来一次

**最正确的决定**：先做 MVP（CLI 单页导出），2 小时跑通核心链路。后续所有功能——水印、密码、Web UI、桌面应用——全是在这个基础上叠加的。如果一开始就设计"支持三种入口、六种后处理、跨平台打包"的架构，大概率花两天画图然后不了了之。

**最昂贵的学习**：Electron → Tauri 的 6 小时迁移。一开始选 Electron 不是因为不知道 Tauri 更轻——是我需要"先用起来"。如果不是先做出了 92MB 的版本亲眼看到问题，我不会真正理解"体积是用户体验的一部分"这句话。这两件事合在一起，说明的是同一套做事方式：**先用最小的成本验证核心假设，验证通过了再追加投资。发现不对，就果断改。跟一个 MVP 死磕，比在方案阶段纠结半天强。**

**后续想做但没做的**：CI/CD 自动构建（目前手动打安装包上传）、自动化测试（目前人工验证）、macOS 支持（Tauri 框架本身跨平台，但只在 Windows 测试过）。

---

## 7. 能力迁移：从写工具到做运营

这段经历看起来是写了一个工具，但底层的做事逻辑和游戏运营是相通的。

| 项目中的实践 | 对运营工作的意义 |
|------|------|
| 两天内把一个想法变成三种形态的完整产品并发布 | 运营同样需要项目闭环能力——从发现玩家流失信号，到提出假设、设计干预、看数据反馈、迭代优化。这是一个完整的"从零到一"周期 |
| 三个方案实测对比后选 Puppeteer——不是看文档，是看输出 | 运营决策需要依据——活动方案 A 还是 B，应该测过再定，不是凭感觉。数据比文档可靠 |
| 做了 CLI / Web / 桌面三种形态 | 运营内容要考虑受众——核心玩家的公告和泛用户的推送，是两种语言、两种渠道 |
| 发现第一版 92MB 后果断迁移框架，6 小时 | 活动效果不好时，敢承认"方案不对"并调整，比硬撑更划算 |
| 把 AI 用到实际工作流里，不只"用过"，是交付了完整产品 | 运营同样需要"AI 当杠杆，人做判断"——比如用 AI 生成活动文案或分析社区舆论情绪，人根据玩家画像和品牌调性做筛选和决策 |

加入凉屋游戏后，我想把这种"从真问题出发、快速验证、AI 加速"的工作方式带到运营一线。从写一个小工具到策划一次玩家活动，底层的做事逻辑是一样的：**发现真实问题 → 最小成本验证 → 迭代优化 → 让结果说话**。

---

*项目时间线：2026-06-03 至 2026-06-05，单人完成全部工作。产出 CLI 工具、Web UI、Tauri 桌面应用三种形态。活跃开发时间约 18-22 小时。GitHub 仓库：[github.com/Raaain9527/html-pdf](https://github.com/Raaain9527/html-pdf)*

*> 附：技术版详细记录（含全部 3 个 Bug 的排查过程、渲染引擎选型详述、Electron vs Tauri 迁移细节）见 `AI作品展示报告_html-pdf_v1.md`*
