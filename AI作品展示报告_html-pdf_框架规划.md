# 《HTML → PDF 矢量导出工具》— AI 作品展示报告 · 框架规划 v1.0

## 报告定位

投递附件 / 作品集，篇幅 ~2,500-3,000 字 + 技术对比表格 + 架构图，适合 4-5 页阅读。

**核心叙事**：

> 求职过程中我需要频繁将 HTML 简历导出为 PDF，但浏览器「另存为 PDF」产出的栅格图文字无法被 ATS 解析，长页面还会被截断。市面上没有工具同时满足"矢量化 + 长页面单页 + 中文水印"。于是我自己动手，4 天内从零到可发布产品，覆盖 CLI、Web UI、桌面应用三种形态。以下记录的是遇到的具体问题、做过的技术决策、以及 AI 在哪些环节充当了杠杆。

叙事主线：**从真实痛点出发，自己驱动自己完成一个完整产品**。技术细节是为叙事服务的，不是炫技。

---

## 结构（共 7 节）

### 1. 痛点与动机（~300 字）

- **具体场景**：投递简历时，我的 HTML 简历在浏览器「另存为 PDF」后，文字变成不可选中的图片，ATS 系统无法解析
- **长页面截断**：三屏长的 HTML 页面被截成三页 PDF，中间还有空白
- **现有方案逐个排除**：wkhtmltopdf（Flex/Grid 不兼容）、浏览器打印（分页不可控）、在线转换工具（有水印/页数限制/隐私顾虑）
- **关键心理**：不是"我想练技术"，是"我真的需要这个东西"。这种自驱和接到需求再执行完全不一样
- 明确说明这不是课程作业或他人布置的任务——完全由自身痛点驱动

### 2. 时间线与产出（~350 字 + 表格）

- 项目集中在 2026/06/03-05，跨度 3 天，活跃开发时间约 18-22 小时
- 按版本迭代展示，体现"快速出 MVP → 逐步堆叠功能"的做事方式

| 时间 | 版本 | 交付内容 |
|------|------|------|
| 06/03 下午 | v0.1 MVP | CLI 工具：输入 HTML → 单页矢量 PDF，基于 Puppeteer + Chromium |
| 06/03 晚间 | v0.2 | 批量导出、URL 输入、自定义页面尺寸、Web UI (Express) |
| 06/04 凌晨 | v0.4 | 水印平铺（含 CJK 字体嵌入）、密码保护、PDF 元数据 |
| 06/04 下午 | v1.0 | Electron 桌面应用（92MB）、拖拽导出 |
| 06/04 晚间 | v1.1.0 | 迁移 Tauri（20MB，4.6x 缩小）、Rust 后端、文件拖拽修复 |
| 06/05 | v1.1.1 | 捆绑 Node.js 便携版、隐藏控制台弹窗、Web 端中文乱码修复 |

- 补充：版本号背后的真实节奏——v0.1 从 cli 到第一个可用 PDF 大约 2 小时，之后每个版本迭代 1-3 小时。v1.0 Electron → v1.1 Tauri 迁移是最大的单次改动，涉及框架替换、Rust 学习、IPC 机制重写，约 6 小时。

### 3. 关键技术与决策（~500 字）

挑 3 个有代表性的技术决策，每个都有"为什么选了 A 而不是 B"的判断过程：

**决策 1：渲染引擎——Puppeteer vs wkhtmltopdf vs WeasyPrint**

| 方案 | 优点 | 缺点 | 为什么没选 |
|------|------|------|------|
| Puppeteer | CSS3 完美兼容，矢量输出 | 依赖 Chromium | ✅ 选用 |
| wkhtmltopdf | 轻量，命令行友好 | Flex/Grid 不支持，2016 年后停维 | 简历里大量 Flex 布局直接乱掉 |
| WeasyPrint | 纯 Python，轻量 | 无 JS 支持，复杂 CSS 兼容差 | 拿真实简历 HTML 测过，圆角和阴影全丢 |

判断依据不是看文档，是把自己的简历 HTML 分别喂给三个方案，实际对比输出效果。Puppeteer 是唯一一个排版和浏览器一致的。

**决策 2：桌面框架——Electron (92MB) → Tauri (20MB)**

第一版用 Electron，3 小时搭完。但看到 92MB 的 exe 和一个全白屏的窗口，意识到这条路不对。查了 Tauri 的文档和对比数据，决定迁移。

迁移过程中的实际困难：Rust 和 Tauri v2 的文档不够完善，`blocking_pick_file` 在 async 函数里 COM 初始化失败导致对话框弹不出来，这个问题花了 3 个小时排查——最后通过 Agent 代码审查定位到根因。文件拖拽也因为 `__TAURI__` 注入机制在不同的加载协议下行为不一致，试了三种方案才稳定。

| | Electron v1.0 | Tauri v1.1 |
|------|------|------|
| exe 大小 | 92 MB | 20 MB |
| 运行时内存 | 360 MB | 44 MB |
| 进程数 | 4 | 2 |
| 捆绑浏览器 | Chromium (~80MB) | 系统 WebView2 (0MB) |

**决策 3：后处理管道——保留 Node.js 而非纯 Rust**

水印、密码加密、元数据这些功能在 JS 生态里用 pdf-lib 两天就做完了。如果用 Rust 重写，需要找等价的 PDF 操作库、处理 CJK 字体嵌入、实现 AES 加密——预估 3-5 天。在"快速出产品"和"架构纯粹性"之间选了前者。

做了妥协：捆绑 Node.js 便携版到安装包（+88MB），但用 `CREATE_NO_WINDOW` 隐藏了控制台弹窗。在 PRD 里把"Chrome 直调模式"列入了后续计划。

### 4. 产品形态：三种入口（~300 字）

项目最终提供了三种使用方式，覆盖不同场景：

```
┌──────────┐  ┌──────────┐  ┌──────────────┐
│ CLI 命令行 │  │ Web UI   │  │ 桌面应用(Tauri)│
│ 开发者/脚本│  │ 浏览器拖拽│  │ 普通用户双击  │
└────┬─────┘  └────┬─────┘  └──────┬───────┘
     │             │              │
     └─────────────┼──────────────┘
                   │
          ┌────────▼────────┐
          │  html2pdf-lib   │  ← 核心引擎只有一份
          │  (Puppeteer)    │
          └────────┬────────┘
                   │
     ┌─────────────┼─────────────┐
     ▼             ▼             ▼
  Chromium     pdf-lib       Node.js
  (页面渲染)   (后处理)       (便携版)
```

三种形态共享同一套核心引擎（`html2pdf-lib.js`，约 140 行），各自只是入口不同。CLI 给开发者和脚本调用，Web UI 给偶尔使用的场景，桌面应用给日常高频用户。

### 5. 踩坑与调试：三个真实 Bug（~400 字）

选 3 个有代表性的 Bug，每个说明"现象 → 排查过程 → 根因 → 修复"：

**Bug 1：Windows 文件对话框不弹出（Tauri 桌面端）**

| | |
|------|------|
| 现象 | 点击「导出 PDF」后界面显示 loading，但文件选择框从未出现，过几秒报错 |
| 排查 | 前端日志显示 invoke 调用已发出，Rust 端加了 eprintln 却无输出——说明 Rust 函数根本没收到调用 |
| 根因 | Rust 函数声明为 `async fn`，在 Tokio 异步线程上调用 `blocking_pick_file()`，Windows 要求文件对话框的调用线程必须初始化 COM STA 模式，Tokio 线程池没做这个初始化，对话框创建静默失败 |
| 修复 | 把 `async fn` 改成 `fn`，Tauri 自动将同步命令调度到有 COM 支持的线程上执行 |
| 耗时 | ~3 小时，中间试过调整线程模型、加 spawn_blocking 都没用，最后通过 Agent 系统审查全链路代码才定位到根因 |

**Bug 2：Web 端导出的 PDF 显示原始 HTML 代码**

| | |
|------|------|
| 现象 | Web UI 上传 HTML 文件导出后，PDF 里是 `<html><head>...` 源代码，不是渲染后的页面 |
| 排查 | CLI 导出同样的文件正常，说明渲染引擎没问题 |
| 根因 | Express 的 multer 中间件把上传文件保存为无后缀的临时名（如 `uploads/a1b2c3d4`），Chrome 加载 `file:///path/to/a1b2c3d4` 时不知道这是 HTML，按 text/plain 渲染 |
| 修复 | 在传入 Puppeteer 之前把临时文件重命名为 `.html` 后缀 |
| 耗时 | ~15 分钟——排查花了 10 分钟，修复一行代码 |

**Bug 3：Tauri 文件拖拽完全无响应**

| | |
|------|------|
| 现象 | 从资源管理器拖 HTML 文件到桌面应用窗口，什么都没发生 |
| 排查 | 前端 JS 的 `drop` 事件中 `e.dataTransfer.files[0].path` 是 `undefined`——Tauri 的 WebView2 不像 Electron 那样给 File 对象加 path 属性 |
| 根因 | Tauri v2 的拖拽事件需要在 Rust 层用 `on_window_event` 监听 `WindowEvent::DragDrop`，然后通过 `emit()` 把文件路径发到前端 |
| 修复 | 在 Rust 的 setup 闭包里注册窗口事件监听，前端用 `window.__TAURI__.event.listen('file-dropped')` 接收路径 |
| 耗时 | ~2 小时，试了 JS 层的 dataTransfer API → 失败，试了 Tauri dialog plugin 的 JS 端 API → 权限不足，最后定位到必须从 Rust 层监听 |

### 6. AI 的使用方式（~350 字）

这个项目里 AI 不是"一键生成"，而是在具体环节充当杠杆。记录几个真实场景：

**PRD 生成**：我描述了需求（HTML 导出 PDF、长页面单页、矢量化），AI 产出了结构化 PRD，包含功能需求表、版本规划、技术方案对比。我修改了技术推荐（从 wkhtmltopdf 换成 Puppeteer，因为实际测过不兼容），调整了优先级。

**代码实现**：CLI 的核心逻辑（Puppeteer 启动 → 加载页面 → 计算高度 → printToPDF）由 AI 生成 v0，我测试后发现两个问题：默认视口 800px 导致我的简历排版被挤压（需要 ≥ 1080px）、`file://` URL 在 Windows 上的反斜杠不兼容。修复后形成可用版本。

**Agent 代码审查**：Tauri 迁移过程中文件对话框不弹出的问题，我排查了 1 个多小时没定位。把完整代码交给 Agent 做系统性审查，它遍历了 `tauri.conf.json` → `lib.rs` → `main.rs` → `capabilities` → 前端 HTML 的整条链路，指出 `blocking_pick_file()` 在 `async fn` 中的 COM 线程问题是根因。这不是 AI 比我聪明——是我有盲区，而它能在一分钟内并行检查整条链路。

**迭代节奏**：大部分 Bug 的闭环是"描述现象 → AI 给方案 → 我验证 → 不行就再描述 → AI 改方案"。短的 10 分钟，长的 3 小时。关键不是 AI 每次都对，是反馈循环比"Google → 看文档 → 试 → 失败 → 换关键词再搜"快一个数量级。

### 7. 反思与能力体现（~400 字）

**最正确的决定**：先按自己的实际需求做 MVP（CLI 单页导出），而不是一开始就规划 Web UI 和桌面应用。MVP 2 小时跑通，验证了核心链路可行，后续功能都是在这个基础上叠加。如果一开始就设计"完美架构"，大概率会花两天画图然后不了了之。

**最浪费的时间**：Electron → Tauri 迁移。如果一开始就调研桌面框架选型，可以省掉那 6 小时。但换个角度，如果不是先用了 Electron 看到 92MB 的成品，我也不会意识到体积是真实问题——人是看到糟糕的结果后才有动力去找更好的方案。

**能力体现**：

| 能力维度 | 项目中的对应实践 |
|------|------|
| **自驱能力** | 没有产品经理、没有设计稿、没有需求文档——痛点是我自己的，产品也是我自己的。从"我需要这个"到"别人也能用"，全程自我驱动 |
| **产品思维** | 把个人需求抽象为三种产品形态：CLI（开发者）、Web UI（偶尔使用）、桌面应用（日常高频）。不是做一个自己能用的脚本就完了 |
| **技术决策力** | Puppeteer vs wkhtmltopdf vs WeasyPrint 的实测对比；Electron vs Tauri 的体积/内存权衡；JS 后处理 vs 纯 Rust 的开发效率取舍 |
| **工程落地力** | 3 天从零到可发布产品，覆盖渲染引擎、后处理管道、CLI、Web 服务、桌面应用五个技术层 |
| **AI 协作能力** | 知道什么时候让 AI 做体力活（代码生成、方案对比），什么时候必须自己判断（技术选型实测、Agent 审查结果的取舍） |

**后续想做但没做的**：

- Chrome 直调模式：Rust 直接调用系统 Chrome `--print-to-pdf`，彻底移除 Node.js 依赖。不做不是因为技术上不可行，而是因为水印/密码/元数据的后处理在 JS 生态里已经成熟，Rust 重写的成本（预估 3-5 天）在当前阶段不划算
- CI/CD 自动构建：每次 push 自动打 Tauri 安装包并上传到 GitHub Release
- macOS 支持：Tauri 框架本身跨平台，但当前只在 Windows 上测试过

---

## 总篇幅

~2,800 字正文 + 4 个表格 + 1 张架构图 + 3 个 Bug 详述。适合 4-5 页阅读。

---

*规划版本：v1.0 | 2026-06-05 | 基于真实项目记录*
