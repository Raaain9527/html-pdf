# HTML → PDF 导出工具 PRD

## 1. 项目背景与目标

将任意 HTML 页面导出为高质量矢量 PDF。核心卖点：**长页面自适应单页、矢量文字可选中、排版忠实还原**。

## 2. 目标用户

| 用户角色 | 典型场景 |
|---------|---------|
| 求职者 | HTML 简历 → 单页 PDF，文字可被 ATS 解析 |
| 运营/商务 | 活动页、数据报表 → PDF 发送客户 |
| 开发者 | API 文档、笔记、CHANGELOG → 批量导出 |
| 普通用户 | 网页文章、教程 → PDF 离线阅读 |

## 3. 功能需求

### 3.1 核心功能 ✅ 已实现

| 编号 | 功能 | 描述 |
|------|------|------|
| F1 | HTML → PDF 导出 | 输入 HTML 文件或 URL，输出矢量 PDF |
| F2 | 长页面单页导出 | 自动计算内容高度，整页渲染不分页 |
| F3 | 矢量文字 | Skia/PDF 引擎，文字可选中、复制、搜索 |
| F4 | 中文字体 | 自动嵌入 CJK 字体子集 |
| F5 | CSS 排版保真 | Flex/Grid/阴影/渐变 完整支持 |
| F6 | 图片/背景 | `<img>`、`background-image`、SVG 正常渲染 |
| F7 | 页面尺寸 | 支持自定义宽高（mm / px），留空自动检测 |
| F8 | 水印 | 平铺文字水印，CJK 字体自动嵌入，透明度/旋转可调 |
| F9 | 密码保护 | PDF 打开密码 + 所有者密码 |
| F10 | 元数据 | 标题/作者/主题/关键词 |
| F11 | URL 导出 | 网页 URL 直接导出 |
| F12 | 批量导出 | CLI 支持多文件/URL 批量处理 |

### 3.2 交互方式

| 编号 | 方式 | 状态 | 描述 |
|------|------|------|------|
| I1 | CLI | ✅ | `node html2pdf.js input.html -o output.pdf` |
| I2 | Web UI | ✅ | `npm run ui` → 浏览器拖拽上传 |
| I3 | 桌面应用 | ✅ | Tauri v2 + Rust，20MB exe，系统 WebView2 |

### 3.3 后续开发计划

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| P1 | **实时预览** | 导出前渲染预览，视口宽度/缩放等参数变化时预览实时更新 | **P0** |
| P2 | **交互式导出** | 支持在预览中点击页面元素（展开折叠区、切换标签等）后再导出，而非仅导出默认 HTML 状态 | **P0** |
| P3 | Chrome 直调模式 | Rust 直接调用 Chrome `--headless --print-to-pdf`，移除 Node.js 依赖 | P2 |
| P4 | PDF 书签 | 根据 HTML h1-h6 自动生成 PDF 目录大纲 | P3 |
| P5 | 图片水印 | 支持 PNG/JPEG 图片作为水印 | P3 |
| P6 | macOS 支持 | 适配 macOS 桌面端 | P3 |

---

### 3.4 P1·P2 详细设计：实时预览 + 交互式导出

#### 问题定义

当前版本点击「导出 PDF」后直接调用 Puppeteer 渲染，用户在导出前看不到页面实际会渲染成什么样。视口宽度设错了、缩放不合适，只能导出后打开 PDF 才知道——然后重新调参、重新导出。这是"盲操作"。

同时，有些 HTML 页面包含交互元素——标签切换面板、折叠手风琴、按需加载的区域——用户希望在导出前先点击展开某些内容，然后导出交互后的最终状态。当前版本只能导出 HTML 的默认渲染状态，无法满足这个需求。

#### UI 布局方案

桌面端（Tauri）推荐采用**左右分栏**布局，Web 端采用**上下分栏**布局：

```
┌─────────────────────────────────────────────────┐
│  HTML → PDF                        [预览] [导出] │
├─────────────────┬───────────────────────────────┤
│                 │                               │
│  📄 文件选择     │                               │
│  ┌─────────────┐│                               │
│  │ 拖拽或点击   ││       实时预览区域              │
│  └─────────────┘│       (iframe / WebView)       │
│                 │                               │
│  已选: resume.html                              │
│                 │      以当前视口宽度渲染          │
│  ⚙ 视口宽度     │      缩放比例实时生效            │
│  [1280px    ▼] │                               │
│                 │      交互模式开关:              │
│  ⚙ 缩放比例     │      [🔒 只读预览]              │
│  [1.0x     ▼] │      [🖱 交互模式] ← 可点击元素  │
│                 │                               │
│  ⚙ 页面尺寸     │                               │
│  [自动     ▼] │                               │
│                 │                               │
│  ▼ 高级选项     │                               │
│  (水印/密码)    │                               │
│                 │                               │
├─────────────────┴───────────────────────────────┤
│  状态栏: 1280px × 3589px · 交互模式已启用          │
└─────────────────────────────────────────────────┘
```

**布局要点**：

- 左侧面板（~380px 固定宽）：文件选择、参数设置、高级选项。与当前 UI 保持一致
- 右侧预览区（flex 填充剩余空间）：嵌入一个缩放后的页面预览，按视口宽度渲染。预览区顶部有工具栏：缩放滑块（25%-200%）、交互模式开关、刷新按钮
- 底部状态栏：实时显示当前视口宽度 × 预估高度，交互模式是否启用
- 预览上方有两个按钮：「🖱 交互模式」（切换预览是否可点击）、「↻ 刷新」（重新加载预览）

**交互模式开关**：

- 默认状态：预览区为只读——鼠标滚轮可滚动，但点击事件被阻止。这是导出前确认排版效果的"所见即所得"模式
- 开启交互模式：预览区变为可交互——用户可以点击标签切换面板、展开折叠区域、触发页面内的按钮。页面内的 JS 正常运行。此时状态栏显示黄色提示"交互模式已启用——导出将捕获当前交互状态"
- 导出时：如果交互模式被启用过，导出的是用户交互后的 DOM 当前状态，而非原始 HTML

#### 技术方案

**P1 实时预览——方案选择**：

| 方案 | 桌面端 (Tauri) | Web 端 (浏览器) | 复杂度 |
|------|------|------|--------|
| **A: iframe 嵌入** | iframe 加载本地 file:// URL 或 tauri:// 协议文件 | iframe 加载 blob URL 或 sandbox | 低 |
| **B: Tauri 多 WebView** | 两个独立 WebView 窗口，通过 IPC 同步 | 不可用 | 高 |
| **C: Puppeteer 截图** | 每次参数变化触发 Puppeteer 截图，显示图片 | fetch 服务端渲染截图 | 中 |

**推荐方案 A（桌面端）**：

- 右侧预览区使用一个 `<iframe>` 加载选中的 HTML 文件
- 监听左侧面板参数变化（视口宽度、缩放），动态调整 iframe 容器的 `width` 和 CSS `transform: scale()`
- 例如视口宽度 1280px、缩放 1.0x → iframe 容器宽度为 `1280 * scale` 像素
- 使用 `debounce(300ms)` 避免高频重载

**推荐方案 A（Web 端）**：

- 上传 HTML 后，将文件内容读取为字符串，通过 `URL.createObjectURL(blob)` 创建临时 URL
- 右侧 `<iframe>` 的 `src` 指向该 blob URL
- 参数变化时通过 `iframe.contentWindow.postMessage()` 或直接操作 iframe DOM 调整视口宽度
- 安全限制：iframe 需要 `sandbox="allow-scripts allow-same-origin"`

**P2 交互式导出——技术方案**：

```
┌──────────────────────────────────────────────────┐
│                   用户操作流程                      │
├──────────────────────────────────────────────────┤
│  1. 选择文件 / 输入 URL                           │
│       │                                          │
│       ▼                                          │
│  2. 预览区加载 HTML（只读模式）                     │
│       │                                          │
│       ▼                                          │
│  3. 用户开启「交互模式」                           │
│       │  └─ 预览区变为可交互                       │
│       ▼                                          │
│  4. 用户在预览中操作（点击标签、展开折叠区、填写表单）  │
│       │                                          │
│       ▼                                          │
│  5. 点击「导出」→ 捕获当前 DOM 状态                 │
│       │  └─ 序列化 iframe 的 document.documentElement.outerHTML
│       ▼                                          │
│  6. 将捕获的 HTML 传给 Puppeteer 渲染为 PDF         │
└──────────────────────────────────────────────────┘
```

**桌面端（Tauri）实现**：

1. 预览区 iframe 在交互模式下，用户可以点击页面元素
2. 导出时，通过 `iframe.contentWindow.document.documentElement.outerHTML` 获取交互后的完整 HTML
3. 将此 HTML 写入临时文件（保留原始文件的同级目录，以保证相对路径的资源引用不丢失）
4. Puppeteer 加载这个临时 HTML 文件进行渲染和导出
5. 导出完成后清理临时文件

关键技术点：
- 写入临时文件时必须放在原始 HTML 的同级目录下，否则 CSS/JS/图片的相对路径引用会失效
- 如果原始文件路径为 `C:\project\index.html`，临时文件为 `C:\project\__preview_export_XXXXX.html`
- 导出后自动清理

**Web 端实现**：

1. 与桌面端逻辑相同：从预览区 iframe 捕获 DOM → 转为 HTML 字符串
2. 如果原始 HTML 引用了外部 CSS/JS（`<link href="https://cdn...">`），这些外部资源会保留在捕获的 HTML 中
3. 将捕获的 HTML 通过 `POST /api/export/interactive` 传给服务端（作为原始 HTML 内容，非文件上传）
4. 服务端用 `page.setContent(html)` 替代 `page.goto(file://...)` 加载，然后正常渲染导出

**关键边界问题**：

| 问题 | 处理方式 |
|------|------|
| 交互后 iframe 内的 JS 状态（变量、事件监听）是否保留 | 不保留。只捕获 DOM 结构和内联样式。交互导致的 DOM 变更（新增元素、修改属性、展开隐藏区域）会保留，但 JS 运行时状态不保留 |
| CSS `:hover`、`:focus` 状态 | 开启交互模式后，用户在预览区点击时自然触发这些伪类。导出前需保持最后交互状态——可在导出瞬间给 body 加一个标记类名，并用 CSS 锁定关键状态 |
| 页面内有 `<script>` 动态生成内容 | 如果 script 在页面加载时自动执行（如 SPA 路由），交互模式下的预览已经执行了这些脚本。导出时捕获的 DOM 是脚本执行后的结果 |
| 原始 HTML 的相对路径资源 | 临时文件放在原始文件同级目录，相对路径不受影响 |
| Web 端上传的 HTML 引用本地图片 | 上传文件的原始路径在服务端不可用——相对路径资源会丢失。Web 端的交互导出仅适用于资源通过绝对 URL（CDN）或内联 base64 引用的页面 |

#### 实现优先级

1. **Phase 1**（P1 基础）：左右分栏布局 + iframe 预览 + 参数变化时 iframe 尺寸实时更新。只读模式。导出仍用原始 HTML
2. **Phase 2**（P1 完善）：预览区工具栏（缩放滑块、刷新按钮）、debounce 优化
3. **Phase 3**（P2 基础）：交互模式开关 + DOM 捕获 + 临时文件写入 + 页面内交互后导出
4. **Phase 4**（P2 完善）：CSS 状态锁定、Web 端 `page.setContent()` 支持、边界情况处理

## 4. 技术方案

| 组件 | 技术 | 说明 |
|------|------|------|
| PDF 渲染 | Puppeteer + Chromium | `Page.printToPDF`，CSS 完美兼容 |
| PDF 后处理 | pdf-lib-plus-encrypt | 水印平铺、AES 加密、元数据 |
| 桌面框架 | Tauri v2 | Rust 后端 + WebView2，20MB exe |
| Web 服务 | Express | 浏览器端上传/导出 |
| CLI | Commander + puppeteer-core | 命令行批量导出 |
| 字体 | @pdf-lib/fontkit | 中文字体嵌入（SimHei） |

## 5. 架构

```
┌─────────────────────────────────────────────┐
│                  用户界面                      │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ Tauri 桌面 │  │ Web UI   │  │ CLI 命令行  │ │
│  │ (Rust壳)  │  │(Express) │  │ (Commander)│ │
│  └────┬──────┘  └────┬─────┘  └─────┬──────┘ │
│       │              │              │         │
│       └──────────────┼──────────────┘         │
│                      │                        │
│              ┌───────▼────────┐               │
│              │  html2pdf-lib  │               │
│              │  (Puppeteer)   │               │
│              └───────┬────────┘               │
│                      │                        │
│         ┌────────────┼────────────┐           │
│         ▼            ▼            ▼           │
│    ┌─────────┐ ┌──────────┐ ┌─────────┐      │
│    │Chromium │ │pdf-lib   │ │ Node.js │      │
│    │(渲染)   │ │(后处理)  │ │(便携版) │      │
│    └─────────┘ └──────────┘ └─────────┘      │
└─────────────────────────────────────────────┘
```

## 6. 版本历史

| 版本 | 日期 | 内容 |
|------|------|------|
| v0.1 | 2026-06 | CLI 工具 MVP |
| v0.2 | 2026-06 | URL 输入、批量导出、自定义尺寸 |
| v0.3 | 2026-06 | Web UI、Express 服务 |
| v0.4 | 2026-06 | 水印、密码保护、PDF 元数据 |
| v1.0 | 2026-06 | Electron 桌面应用 |
| v1.1 | 2026-06 | 迁移 Tauri (20MB)、捆绑 Node.js 便携版、隐藏控制台 |

## 7. 成功指标

- [x] PDF 文字可选中复制（Skia/PDF 矢量输出）
- [x] 排版与浏览器一致度 ≥ 95%
- [x] 长页面单页无截断
- [x] 桌面应用 20MB，内存 44MB
- [x] 免安装，无 Node.js 依赖（已内置便携版）
